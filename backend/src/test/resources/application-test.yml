spring:
  datasource:
    url: jdbc:mysql://localhost:3307/iglootestdb?useSSL=false&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true
    username: root
    password: root1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: localhost
      port: 6379
      password: 1234
      redisson:
        connection-pool-size: 1000
        connection-minimum-idle-size: 20
        timeout: 3000
  cloud:
    aws:
      credentials:
        access-key: test-access-key
        secret-key: test-secret-key
      s3:
        bucket: test-bucket
  mail:
    username: test@example.com
    password: test-password
  toss:
    secret-key: test-secret-key
    client-key: test-client-key

jwt:
  secret: test-jwt-secret-123456789012345678901234567890

sweettracker:
  api-key: test-api-key

kakao:
  map:
    rest-api-key: test-map-key
